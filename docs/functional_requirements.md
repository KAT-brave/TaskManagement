# 機能要件

## 1. 認証機能

- ユーザー登録（名前・メールアドレス・パスワード）
- ログイン / ログアウト
- 自分のデータのみ表示・操作できる

## 2. ボード機能

- ボードの作成・編集・削除
- ボード一覧の表示

## 3. リスト機能

- リストの作成・編集・削除
- ボード内に複数のリストを表示

## 4. カード機能

- カードの作成・編集・削除
- タイトルと説明文・期限日を持つ

## 5. カード移動機能

- ドラッグ&ドロップでリスト間を移動できる

## 6. カード並び替え機能

- 「優先度順」ボタンで優先度が高い順に並び替えできる
- 「期限順」ボタンで期限日が近い順に並び替えできる
- 「フリー」ボタンでドラッグ&ドロップによる自由な並び替えに戻る
- 並び替えボタンを押した後もドラッグ&ドロップで自由に並び替え可能

---

## 7. URL設計（REST API）

フロントエンドはSPAのため、バックエンドはすべて `/api/` プレフィックスのREST APIとして提供する。

### 認証（未実装・今後対応予定）

| やりたいこと | エンドポイント | メソッド |
|---|---|---|
| ユーザー登録 | /api/auth/register | POST |
| ログイン | /api/auth/login | POST |
| ログアウト | /api/auth/logout | POST |

### ボード（未実装・今後対応予定）

| やりたいこと | エンドポイント | メソッド |
|---|---|---|
| ボード一覧取得 | /api/boards | GET |
| ボード作成 | /api/boards | POST |
| ボード更新 | /api/boards/{id} | PUT |
| ボード削除 | /api/boards/{id} | DELETE |

### リスト（未実装・今後対応予定）

| やりたいこと | エンドポイント | メソッド |
|---|---|---|
| リスト一覧取得 | /api/lists | GET |
| リスト作成 | /api/lists | POST |
| リスト更新 | /api/lists/{id} | PUT |
| リスト削除 | /api/lists/{id} | DELETE |

### カード（実装済み）

| やりたいこと | エンドポイント | メソッド | 状態 |
|---|---|---|---|
| カード一覧取得 | /api/cards | GET | ✅ 実装済み |
| カード1件取得 | /api/cards/{id} | GET | ✅ 実装済み |
| カード作成 | /api/cards | POST | ✅ 実装済み |
| カード更新 | /api/cards/{id} | PUT | ✅ 実装済み |
| カード削除 | /api/cards/{id} | DELETE | ✅ 実装済み |

---

## 8. ユースケース / 操作フロー

### タスクを作成して完了にするまでの流れ

1. ログイン画面からログインする
2. ボード一覧画面で対象のボードをクリックする
3. 「Todo」リストの「+ カード」をクリックする
4. タイトルと説明を入力して保存する
5. 作成されたカードをドラッグして「進行中」リストに移動する
6. タスクが完了したらカードを「完了」リストにドラッグして移動する

### ボードを新しく作る流れ

1. ボード一覧画面で「+ 新規作成」をクリックする
2. ボード名を入力して保存する
3. 作成されたボードの詳細画面に遷移する
4. 「+ リスト追加」でリストを作成する

### ゲストとしてログインする流れ

1. ログイン画面の「ゲストログイン」ボタンをクリックする
2. デモアカウントで自動ログインされる
3. ボード一覧画面に遷移する
